#!/bin/bash
# Claude Code Aliases - Source this file at the start of your sessions
# Usage: source /c/Users/Corbin/development/.claude_aliases

# ============================================
# NAVIGATION ALIASES
# ============================================
alias dev='cd /c/Users/Corbin/development'
alias shared='cd /c/Users/Corbin/development/shared'
alias proj='cd /c/Users/Corbin/projects'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias home='cd ~'

# ============================================
# LISTING ALIASES
# ============================================
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias lt='ls -alt'  # Sort by time
alias lsize='ls -alSh'  # Sort by size

# ============================================
# GIT SHORTCUTS
# ============================================
alias gs='git status'
alias ga='git add'
alias gaa='git add --all'
alias gc='git commit -m'
alias gca='git commit -am'
alias gp='git push'
alias gpl='git pull'
alias gd='git diff'
alias gds='git diff --staged'
alias gl='git log --oneline -10'
alias glog='git log --graph --pretty=format:"%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset" --abbrev-commit'
alias gb='git branch'
alias gco='git checkout'
alias gcb='git checkout -b'
alias gundo='git reset HEAD~1 --soft'

# ============================================
# PYTHON DEVELOPMENT
# ============================================
alias py='python'
alias py3='python3'
alias pyshared='PYTHONPATH="/c/Users/Corbin/development/shared" python'
alias pip='python -m pip'
alias venv='python -m venv'
alias activate='source venv/Scripts/activate'  # Windows Git Bash path
alias deactivate='deactivate 2>/dev/null || true'
alias pipreq='pip freeze > requirements.txt'
alias pipinst='pip install -r requirements.txt'

# ============================================
# YOUR SPECIFIC PROJECTS
# ============================================
alias apigate='PYTHONPATH="/c/Users/Corbin/development/shared" python api_gateway.py'
alias observatory='PYTHONPATH="/c/Users/Corbin/development/shared" python enhanced_observatory_adapter.py'
alias metrics='PYTHONPATH="/c/Users/Corbin/development/shared" python api_gateway_metrics.py'

# ============================================
# NPM/NODE ALIASES
# ============================================
alias ni='npm install'
alias nis='npm install --save'
alias nid='npm install --save-dev'
alias nr='npm run'
alias nrs='npm run start'
alias nrd='npm run dev'
alias nrb='npm run build'
alias nrt='npm run test'
alias nrl='npm run lint'

# ============================================
# DOCKER ALIASES
# ============================================
alias dk='docker'
alias dkps='docker ps'
alias dkpsa='docker ps -a'
alias dki='docker images'
alias dkrm='docker rm'
alias dkrmi='docker rmi'
alias dc='docker-compose'
alias dcu='docker-compose up'
alias dcud='docker-compose up -d'
alias dcd='docker-compose down'
alias dcl='docker-compose logs'
alias dclf='docker-compose logs -f'

# ============================================
# UTILITY ALIASES
# ============================================
alias cls='clear'
alias mkdir='mkdir -p'
alias tree='tree -C'
alias grep='grep --color=auto'
alias df='df -h'
alias du='du -h'
alias free='free -h'
alias ports='netstat -tulanp'
alias myip='curl http://ipecho.net/plain'
alias weather='curl wttr.in'

# ============================================
# CLAUDE CODE SPECIFIC
# ============================================
alias claude-help='echo "Claude Code Aliases loaded! Type alias to see all available aliases"'
alias reload-aliases='source /c/Users/Corbin/development/.claude_aliases && echo "Aliases reloaded!"'

# ============================================
# FUNCTIONS (More powerful than aliases)
# ============================================

# Create and enter directory
mkcd() {
    mkdir -p "$1" && cd "$1"
}

# Git commit with automatic add
gcommit() {
    git add -A && git commit -m "$1"
}

# Find files by name
ff() {
    find . -type f -name "*$1*"
}

# Find directories by name
fd() {
    find . -type d -name "*$1*"
}

# Extract any archive
extract() {
    if [ -f $1 ]; then
        case $1 in
            *.tar.bz2)   tar xjf $1     ;;
            *.tar.gz)    tar xzf $1     ;;
            *.bz2)       bunzip2 $1     ;;
            *.rar)       unrar e $1     ;;
            *.gz)        gunzip $1      ;;
            *.tar)       tar xf $1      ;;
            *.tbz2)      tar xjf $1     ;;
            *.tgz)       tar xzf $1     ;;
            *.zip)       unzip $1       ;;
            *.Z)         uncompress $1  ;;
            *.7z)        7z x $1        ;;
            *)     echo "'$1' cannot be extracted via extract()" ;;
        esac
    else
        echo "'$1' is not a valid file"
    fi
}

# Quick Python server
pyserver() {
    python -m http.server ${1:-8000}
}

# ============================================
# Status messages - using printf to avoid issues
printf "✓ Claude Code aliases loaded! Type 'alias' to see all aliases.\n"
printf "✓ Custom functions available: mkcd, gcommit, ff, fd, extract, pyserver\n"